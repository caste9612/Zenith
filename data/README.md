# Cartella dati

Metti qui il tuo Excel storico con nome **`patrimonio.xlsx`**.

⚠️ I file `*.xlsx`, `*.xls`, `*.csv` di questa cartella sono **esclusi da git** (vedi `.gitignore`): i tuoi dati finanziari non finiranno mai nel repository remoto.

Nella **Fase 0**, Claude Code leggerà questo file per dedurre lo schema dati reale e proporre il piano di import, prima di scrivere codice.
