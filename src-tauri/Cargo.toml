[package]
name = "app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
rust-version = "1.77.2"
# NB: la versione dell'app è in tauri.conf.json ("version"); qui resta solo quella del crate.

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.2" }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.11.2" }
tauri-plugin-log = "2"
# Chiamate HTTP dal layer nativo (niente CORS) per le API di mercato.
tauri-plugin-http = "2"
# Riavvio dopo l'installazione dell'aggiornamento.
tauri-plugin-process = "2"
# Export: finestra "Salva con nome" + scrittura file su desktop.
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"

# Auto-update: solo desktop (su Android l'aggiornamento passa dallo store).
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"
